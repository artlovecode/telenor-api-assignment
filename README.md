# Telenor tech challenge

To start the app, `cd` into the project folder and run `docker-compose up --build`

To run the tests in the project, run `yarn test`

To start the app without docker, run `yarn start`
